;偽酒
@SYSTEM_MESSAGE_502(ARG:0, ARG:1, ARGS:0, ARGS:1, ARG:2, ARG:3, ARG:4, ARG:5, ARG:6)

IF ARG:1 == 0
	PRINTFORML 瓶口碰到了光洁的嘴唇。 
	PRINTFORML %ARGS:0%在这可以悠闲地喝酒的令人安心的悠闲时间中
IF FLAG:AUTOMODE
    PRINTFORML 呼啊啊、叹了一口气。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  呼啊啊、叹了一口气。
ENDIF

ELSEIF ARG:1 == 1
IF FLAG:AUTOMODE
    PRINTFORML 喝着喝着、身体渐渐被惬意的倦怠感充满 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  喝着喝着、身体渐渐被惬意的倦怠感充满 
ENDIF

	PRINTFORML %ARGS:0%还没反应过来身体就倒在了地上、陷入了沉眠。
IF FLAG:AUTOMODE
    PRINTFORML 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 不知为何、盛酒的容器自动爬上了嘴唇、瓶口缠住了嘴唇。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  不知为何、盛酒的容器自动爬上了嘴唇、瓶口缠住了嘴唇。
ENDIF

	PRINTFORML 柔软变形的瓶口、就像享受着里面的触感一样、
	PRINTFORML 反复抽插着、将体内的浑浊酒液喷洒出来。
	PRINTFORML 唾液乱糟糟地涂在瓶口上、拉出一条透明的丝线。
	PRINTFORML 无意识的%ARGS:0%的舌头不知不觉地开始撩拨起瓶口。
	PRINTFORML 
IF FLAG:AUTOMODE
    PRINTFORML %ARGS:0%眼睛一睁、发现已经被微苦的酒液弄脏了脸。      
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  %ARGS:0%眼睛一睁、发现已经被微苦的酒液弄脏了脸。      
ENDIF


ELSEIF ARG:1 == 2
IF FLAG:AUTOMODE
    PRINTFORML 美味
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  美味
ENDIF

	PRINTFORML 
	PRINTFORML 口感和回味都很棒。
IF FLAG:AUTOMODE
    PRINTFORML 不知不觉地喝的越来越快。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  不知不觉地喝的越来越快。
ENDIF

	PRINTFORML 喝酒喝到一半、身体突然开始发热。
IF FLAG:AUTOMODE
    PRINTFORML %ARGS:0%袒露出胸部和大腿、继续喝起酒来。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  %ARGS:0%袒露出胸部和大腿、继续喝起酒来。
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 还不够。更多、还要更多
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  还不够。更多、还要更多
ENDIF

	PRINTFORML 只要吮吸刺激着瓶口、越来越多的美味酒液就会满溢而出。
IF FLAG:AUTOMODE
    PRINTFORML 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  
ENDIF

	PRINTFORML prprprpr、发出着啾啾啾的声音、
	PRINTFORML 嘴角滴落着唾液和白浊的液体%ARGS:0%全神贯注地吸吮着里面的东西。
IF FLAG:AUTOMODE
    PRINTFORML 直到最后都没发现容器在不自然地搏动着。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  直到最后都没发现容器在不自然地搏动着。
ENDIF


ELSEIF ARG:1 == 3

	PRINTFORML 突然容器搏动了起来。
IF FLAG:AUTOMODE
    PRINTFORML 这是触手的拟态。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  这是触手的拟态。
ENDIF

	PRINTFORML 虽然明白了是怎么回事。
IF FLAG:AUTOMODE
    PRINTFORML 但是那瓶子里也有着美味的酒。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  但是那瓶子里也有着美味的酒。
ENDIF

	PRINTFORML 利用拟态是弱者的行为、要这么说服自己、没事的。
IF FLAG:AUTOMODE
    PRINTFORML 应该不会出什么事吧。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  应该不会出什么事吧。
ENDIF

	PRINTFORML 
	PRINTFORML 用唾液湿润着舌头。
	PRINTFORML 一边啧啧有声的吮吸、一边用舌头刺激着顶端。
IF FLAG:AUTOMODE
    PRINTFORML 啾噜啾噜啾噜啾噜
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  啾噜啾噜啾噜啾噜
ENDIF

	PRINTFORML 流出来的酒、那种味道能让人把疲劳一扫而空。
	PRINTFORML 用手指搓揉容器时、浓郁的香味油然而生、酒的味道也变得难以形容起来。 
IF FLAG:AUTOMODE
    PRINTFORML 不知不觉地摇头晃脑、沉浸于其中了。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  不知不觉地摇头晃脑、沉浸于其中了。
ENDIF

	PRINTFORML 
	PRINTFORML 突然回过神来。
IF FLAG:AUTOMODE
    PRINTFORML 里面已经什么都不剩了、急忙放开了瓶子停止了吸吮。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  里面已经什么都不剩了、急忙放开了瓶子停止了吸吮。
ENDIF


ELSEIF ARG:1 == 4
IF FLAG:AUTOMODE
    PRINTFORML  「!?」
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   「!?」
ENDIF

	PRINTFORML 浑身失去了力气。
	PRINTFORML 脑袋觉得不自然、手脚也动不了。
	PRINTFORML 好像是麻药一类的东西。
	PRINTFORML 就这样突然受到了冲击、被推倒了。
IF FLAG:AUTOMODE
    PRINTFORML 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 变成触手的容器在嘴里不停地蠕动着、发出咕啾咕啾的声音。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  变成触手的容器在嘴里不停地蠕动着、发出咕啾咕啾的声音。
ENDIF

	PRINTFORML 想要推开它的手失去了力气、被强行灌入了白色浑浊的酒液。
	PRINTFORML 想用大脑思考对策、身体却不听使唤、思考也因酒精而变得迟钝。
	PRINTFORML 为了尽快得到解放、只能用嘴舔着触手。
IF FLAG:AUTOMODE
    PRINTFORML 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  
ENDIF

	PRINTFORML 直到触手满足为止、对嘴巴的强奸都会持续下去。
	PRINTFORML 那味道出奇的好、让人气愤。
IF FLAG:AUTOMODE
    PRINTFORML ……… 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  ……… 
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  
ENDIF


ELSEIF ARG:1 == 5

IF FLAG:AUTOMODE
    PRINTFORML  「!?」
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   「!?」
ENDIF

	PRINTFORML 突然、%ARGS:0%的双手和脸被从容器伸出的触手拘束了。
IF FLAG:AUTOMODE
    PRINTFORML 好像有什么东西拟态成了酒瓶。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  好像有什么东西拟态成了酒瓶。
ENDIF

	PRINTFORML 粗大的触手、把身体插进了嘴唇之间侵犯着口腔。
	PRINTFORML 即使发出沉闷的悲鸣、触手仍在不停地发狂。
IF FLAG:AUTOMODE
    PRINTFORML 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 突然触手停止了活动。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  突然触手停止了活动。
ENDIF

	PRINTFORML 一边让粘液扯成丝线、一边慢慢地从嘴巴里拔出。
IF FLAG:AUTOMODE
    PRINTFORML 喝光了嘴里的酒、从容地调整呼吸、松了一口气。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  喝光了嘴里的酒、从容地调整呼吸、松了一口气。
ENDIF

	PRINTFORML 
	PRINTFORML 触手在面前晃来晃去。
IF FLAG:AUTOMODE
    PRINTFORML 贮存在体内的酒液也滴落下来。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  贮存在体内的酒液也滴落下来。
ENDIF

	PRINTFORML 咕嘟
	PRINTFORML %ARGS:0%响起了刚刚喝下去的酒液的味道、
IF FLAG:AUTOMODE
    PRINTFORML 像是被引导着一样舔了舔涌出液体的尖端。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  像是被引导着一样舔了舔涌出液体的尖端。
ENDIF

	PRINTFORML 
IF FLAG:AUTOMODE
    PRINTFORML 触手满足地颤抖着、温柔地把身体伸进了嘴里。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  触手满足地颤抖着、温柔地把身体伸进了嘴里。
ENDIF

  
ELSEIF ARG:1 == 6
IF FLAG:AUTOMODE
    PRINTFORML  「!?」
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   「!?」
ENDIF

	PRINTFORML 突然、%ARGS:0%的双手和脸被从容器伸出的触手拘束了。
IF FLAG:AUTOMODE
    PRINTFORML 好像有什么东西拟态成了酒瓶。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  好像有什么东西拟态成了酒瓶。
ENDIF

	PRINTFORML 粗大的触手、把身体插进了嘴唇之间侵犯着口腔。
	PRINTFORML 即使发出沉闷的悲鸣、触手仍在不停地发狂。
IF FLAG:AUTOMODE
    PRINTFORML 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 多库多库多库
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  多库多库多库
ENDIF

	PRINTFORML 被狠狠侵犯后、白浊液体气势凶猛地灌了进去。
IF FLAG:AUTOMODE
    PRINTFORML 从嘴角漏出的白色液体将脸和胸口弄脏了。
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  从嘴角漏出的白色液体将脸和胸口弄脏了。
ENDIF

	PRINTFORML 即使残留在嘴里的体液味道很好、也没有让自己感觉好受些。
IF FLAG:AUTOMODE
    PRINTFORML 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  
ENDIF


ELSEIF ARG:1 == 7
	PRINTFORML

ELSEIF ARG:1 == 8
	PRINTFORML %ARGS:0%刚要喝酒、酒气就弥漫了四周。
IF FLAG:AUTOMODE
    PRINTFORML 还没来得及抵抗的%ARGS:0%%TACHI()%直接失去了意识……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  还没来得及抵抗的%ARGS:0%%TACHI()%直接失去了意识……
ENDIF

	PRINTFORML 仿佛在确认%ARGS:0%%TACHI()%有没有睡着、
IF FLAG:AUTOMODE
    PRINTFORML 装酒的容器伸出了触手分开了%ARGS:0%的腿、将瓶口插进她的阴道内！
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  装酒的容器伸出了触手分开了%ARGS:0%的腿、将瓶口插进她的阴道内！
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 就这样让她喝下了所有的酒后、把昏迷不醒的%ARGS:0%%TACHI()%丢下、不知跑到哪里去了……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  就这样让她喝下了所有的酒后、把昏迷不醒的%ARGS:0%%TACHI()%丢下、不知跑到哪里去了……
ENDIF


ELSEIF ARG:1 == 9
	PRINTFORML %ARGS:0%刚要喝酒、酒气就弥漫了四周。
IF FLAG:AUTOMODE
    PRINTFORML 还没来得及抵抗的%ARGS:0%%TACHI()%直接失去了意识……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  还没来得及抵抗的%ARGS:0%%TACHI()%直接失去了意识……
ENDIF

	PRINTFORML 仿佛在确认%ARGS:0%%TACHI()%有没有睡着、
IF FLAG:AUTOMODE
    PRINTFORML 装酒的容器伸出了触手分开了%ARGS:0%的屁股、将瓶口插进她的菊穴内！
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  装酒的容器伸出了触手分开了%ARGS:0%的屁股、将瓶口插进她的菊穴内！
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 就这样让她喝下了所有的酒后、把昏迷不醒的%ARGS:0%%TACHI()%丢下、不知跑到哪里去了……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  就这样让她喝下了所有的酒后、把昏迷不醒的%ARGS:0%%TACHI()%丢下、不知跑到哪里去了……
ENDIF


ENDIF