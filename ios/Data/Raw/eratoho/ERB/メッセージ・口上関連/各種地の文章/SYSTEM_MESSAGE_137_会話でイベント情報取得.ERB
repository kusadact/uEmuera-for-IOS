;会話でイベント情報取得
@SYSTEM_MESSAGE_137(ARG:0, ARG:1, ARGS:0, ARGS:1, ARG:2, ARG:3, ARG:4, ARG:5, ARG:6)

SELECTCASE ARG:2
	CASE 0
IF FLAG:AUTOMODE
    PRINTFORML (SYSTEM_MESSAGE_137)活动ID不正确 ({ARG:1}, %ARGS:0%, %ARGS:1%, {ARG:2}, {ARG:3}, {ARG:4}, {ARG:5}, {ARG:6})
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  (SYSTEM_MESSAGE_137)イベントのIDが不正です ({ARG:1}, %ARGS:0%, %ARGS:1%, {ARG:2}, {ARG:3}, {ARG:4}, {ARG:5}, {ARG:6})
ENDIF

	CASE 1 TO 99
		;対象キャラの捕食情報
		IF ARG:3 == 0
			PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
			IF FLAG:AUTOMODE
			    PRINTFORML 最近在{CFLAG:(ARG:2):4}号房间附近似乎听到有人在说话…
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{CFLAG:(ARG:2):4}号房间附近似乎听到有人在说话…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{CFLAG:(ARG:2):4}号房间附近似乎听到有人在说话…
ENDIF

			ENDIF

		ELSE
			IF FLAG:AUTOMODE
			    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%处得知、{CFLAG:(ARG:2):4}号房间里%CALLNAME:(ARG:2)%被%SAVESTR:(CFLAG:(ARG:2):10)%抓住的消息
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%处得知、{CFLAG:(ARG:2):4}号房间里%CALLNAME:(ARG:2)%被%SAVESTR:(CFLAG:(ARG:2):10)%抓住的消息
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%处得知、{CFLAG:(ARG:2):4}号房间里%CALLNAME:(ARG:2)%被%SAVESTR:(CFLAG:(ARG:2):10)%抓住的消息
ENDIF

			ENDIF

		ENDIF
	;↓　雑談で得られる情報
	CASE 108
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:108 % 100}号房间里似乎看到了可疑的药物…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:108 % 100}号房间里似乎看到了可疑的药物…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:108 % 100}号房间里似乎看到了可疑的药物…
ENDIF

		ENDIF

	CASE 111
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:111 % 100}号房间里似乎看到了可疑的道具…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:111 % 100}号房间里似乎看到了可疑的道具…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:111 % 100}号房间里似乎看到了可疑的道具…
ENDIF

		ENDIF

	CASE 119
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:119 % 100}号房间里似乎看到了可疑的药物…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:119 % 100}号房间里似乎看到了可疑的药物…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:119 % 100}号房间里似乎看到了可疑的药物…
ENDIF

		ENDIF

	CASE 133
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:133 % 100}号房间里发现了泉水…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:133 % 100}号房间里发现了泉水…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:133 % 100}号房间里发现了泉水…
ENDIF

		ENDIF

	CASE 134
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:134 % 100}号房间里发现了泉水…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:134 % 100}号房间里发现了泉水…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:134 % 100}号房间里发现了泉水…
ENDIF

		ENDIF

	CASE 139
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:139 % 100}号房间里吸入了可疑的气体、吃了不少苦头…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:139 % 100}号房间里吸入了可疑的气体、吃了不少苦头…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:139 % 100}号房间里吸入了可疑的气体、吃了不少苦头…
ENDIF

		ENDIF

	CASE 142
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:142 % 100}号房间里看到了会说话的触手…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:142 % 100}号房间里看到了会说话的触手…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:142 % 100}号房间里看到了会说话的触手…
ENDIF

		ENDIF

	CASE 160
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:160 % 100}号房间里看到了奇怪的装置…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:160 % 100}号房间里看到了奇怪的装置…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:160 % 100}号房间里看到了奇怪的装置…
ENDIF

		ENDIF

	CASE 170
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:170 % 100}号房间里看到了奇怪的植物…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:170 % 100}号房间里看到了奇怪的植物…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:170 % 100}号房间里看到了奇怪的植物…
ENDIF

		ENDIF

	CASE 172
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:172 % 100}号房间里看到了奇怪的装置…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:172 % 100}号房间里看到了奇怪的装置…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:172 % 100}号房间里看到了奇怪的装置…
ENDIF

		ENDIF

	CASE 173
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:173 % 100}号房间里看到了奇怪的植物…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:173 % 100}号房间里看到了奇怪的植物…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:173 % 100}号房间里看到了奇怪的植物…
ENDIF

		ENDIF

	CASE 177
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:177 % 100}号房间里看到了大量的淫虫…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:177 % 100}号房间里看到了大量的淫虫…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:177 % 100}号房间里看到了大量的淫虫…
ENDIF

		ENDIF

	CASE 178
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:178 % 100}号房间里和一个女人做爱了…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:178 % 100}号房间里和一个女人做爱了…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:178 % 100}号房间里和一个女人做爱了…
ENDIF

		ENDIF

	CASE 179
		PRINTFORML 闲谈时、%ARGS:1%说着自己在意的事
		IF FLAG:AUTOMODE
		    PRINTFORML 最近在{FLAG:179 % 100}号房间里看到了有触手在举行仪式…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  最近在{FLAG:179 % 100}号房间里看到了有触手在举行仪式…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   最近在{FLAG:179 % 100}号房间里看到了有触手在举行仪式…
ENDIF

		ENDIF

	
	;↓　情報収集で得られる情報
	CASE 109
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:109}号房间里掉落了什么药物
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:109}号房间里掉落了什么药物
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:109}号房间里掉落了什么药物
ENDIF

		ENDIF

	CASE 110
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%处得知{FLAG:110}号房间里掉落了什么药物
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%处得知{FLAG:110}号房间里掉落了什么药物
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%处得知{FLAG:110}号房间里掉落了什么药物
ENDIF

		ENDIF

	CASE 136
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:136}号房间的天花板上贴着史莱姆
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:136}号房间的天花板上贴着史莱姆
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:136}号房间的天花板上贴着史莱姆
ENDIF

		ENDIF

	CASE 137
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:137}号房间里墙壁上的触手会袭击过来
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:137}号房间里墙壁上的触手会袭击过来
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:137}号房间里墙壁上的触手会袭击过来
ENDIF

		ENDIF

	CASE 138
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:138}号房间里有陷阱
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:138}号房间里有陷阱
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:138}号房间里有陷阱
ENDIF

		ENDIF

	CASE 143
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知{FLAG:143}号房间里充满了不可思议的气体
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:143}号房间里充满了不可思议的气体
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:143}号房间里充满了不可思议的气体
ENDIF

		ENDIF

	CASE 146
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:146}号房间里有淫虫的巢穴
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:146}号房间里有淫虫的巢穴
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:146}号房间里有淫虫的巢穴
ENDIF

		ENDIF

	CASE 147
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:147}号房间里有淫虫的巢穴
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:147}号房间里有淫虫的巢穴
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:147}号房间里有淫虫的巢穴
ENDIF

		ENDIF

	CASE 148
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:148}号房间里有淫虫的巢穴
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:148}号房间里有淫虫的巢穴
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:148}号房间里有淫虫的巢穴
ENDIF

		ENDIF

	CASE 149
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:149}号房间里有淫虫的巢穴
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:149}号房间里有淫虫的巢穴
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:149}号房间里有淫虫的巢穴
ENDIF

		ENDIF

	CASE 150
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:150}号房间里有危险的落石
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:150}号房间里有危险的落石
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:150}号房间里有危险的落石
ENDIF

		ENDIF

	CASE 152
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:152}号房间里有淫虫的巢穴
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:152}号房间里有淫虫的巢穴
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:152}号房间里有淫虫的巢穴
ENDIF

		ENDIF

	CASE 161
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知她曾在{FLAG:161}号房间里和女性做过爱…
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知她曾在{FLAG:161}号房间里和女性在做过爱…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知她曾在{FLAG:161}号房间里和女性在做过爱…
ENDIF

		ENDIF

	CASE 169
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:169}号房间里掉落了什么药物
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:169}号房间里掉落了什么药物
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:169}号房间里掉落了什么药物
ENDIF

		ENDIF

	CASE 180
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:180}号房间里有魔法阵
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:180}号房间里有魔法阵
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知、{FLAG:180}号房间里有魔法阵
ENDIF

		ENDIF

	CASE 206
		IF FLAG:AUTOMODE
		    PRINTFORML %CALLNAME:1%%TACHI()%从%ARGS:1%得知、按照%GET_DUMMY_PLATE_DATA()%的顺序在石版前咏唱咒文的话会遭遇惨痛的经历
		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
IF FLAG:AUTOMODE
    PRINTFORML  %CALLNAME:1%%TACHI()%从%ARGS:1%得知、按照%GET_DUMMY_PLATE_DATA()%的顺序在石版前咏唱咒文的话会遭遇惨痛的经历
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW   %CALLNAME:1%%TACHI()%从%ARGS:1%得知、按照%GET_DUMMY_PLATE_DATA()%的顺序在石版前咏唱咒文的话会遭遇惨痛的经历
ENDIF

		ENDIF

	CASEELSE
		;特に何も表示しない
ENDSELECT

