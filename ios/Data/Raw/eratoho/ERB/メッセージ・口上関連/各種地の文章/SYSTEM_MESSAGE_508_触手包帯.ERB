;触手包帯
@SYSTEM_MESSAGE_508(ARG:0, ARG:1, ARGS:0, ARGS:1, ARG:2, ARG:3, ARG:4, ARG:5, ARG:6)


IF ARG:1 == 0
IF FLAG:AUTOMODE
    PRINTFORML 完全缠上去后、拟态成触手的绷带突然蠕动起来、缠到了%ARGS:0%的胸部上！
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  完全缠上去后、拟态成触手的绷带突然蠕动起来、缠到了%ARGS:0%的胸部上！
ENDIF

	PRINTFORML 要把它扯下来、肩膀和肘关节就会被锁住……
	PRINTFORML 如果硬要扯下来的话、感觉肩膀会被折断……
IF FLAG:AUTOMODE
    PRINTFORML %ARGS:0%放弃了、就把绷带留在了那里……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  %ARGS:0%放弃了、就把绷带留在了那里……
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  
ENDIF


ELSEIF ARG:1 == 1
	IF BASE:3 > MAXBASE:3 / 2 && RAND:3 == 0
IF FLAG:AUTOMODE
    PRINTFORML 绷带在%ARGS:0%的衣服下蠕动着……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  绷带在%ARGS:0%的衣服下蠕动着……
ENDIF

	ELSEIF GET_BUST(TARGET) >= 3 && RAND:2 == 0
IF FLAG:AUTOMODE
    PRINTFORML 绷带从%ARGS:0%丰满的胸部的根部到顶端全都缠绕了上去、开始挤压起来……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  绷带从%ARGS:0%丰满的胸部的根部到顶端全都缠绕了上去、开始挤压起来……
ENDIF

	ELSEIF GET_BUST(TARGET) >= 3 && RAND:2 == 0
IF FLAG:AUTOMODE
    PRINTFORML 绷带紧紧缠绕固定住%ARGS:0%巨大的乳房、连乳头和乳晕都显得更加丰满了一些……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  绷带紧紧缠绕固定住%ARGS:0%巨大的乳房、连乳头和乳晕都显得更加丰满了一些……
ENDIF

	ELSEIF GET_BUST(TARGET) >= 1 && RAND:2 == 0
IF FLAG:AUTOMODE
    PRINTFORML 绷带从%ARGS:0%丰满的胸部的根部到顶端全都缠绕了上去、开始挤压起来……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  绷带从%ARGS:0%丰满的胸部的根部到顶端全都缠绕了上去、开始挤压起来……
ENDIF

	ELSEIF GET_BUST(TARGET) >= 1 && RAND:2 == 0
IF FLAG:AUTOMODE
    PRINTFORML 固定突出着%ARGS:0%的胸部、绷带在乳房周围收紧着……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  固定突出着%ARGS:0%的胸部、绷带在乳房周围收紧着……
ENDIF

	ELSE
IF FLAG:AUTOMODE
    PRINTFORML 像在欺负%ARGS:0%一样、勒紧了她的胸膛…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  像在欺负%ARGS:0%一样、勒紧了她的胸膛…
ENDIF

	ENDIF
ENDIF

