;オープニング
@SYSTEM_MESSAGE_203(ARG:0, ARG:1, ARGS:0, ARGS:1, ARG:2, ARG:3, ARG:4, ARG:5, ARG:6)

;ストーリーを改変したい場合、各口上で地の文書き換えによって改変してください
;例：霊夢口上で、元々異変の事を知っていて自分から乗り込んできたようにしたい　　など

IF FLAG:AUTOMODE
    PRINTFORML 即使是在世间的非常识成为常识的幻想乡……在那样的地方也只能被认为是异常的景象在%NAME:1%的眼前出现
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  即使是在世间的非常识成为常识的幻想乡……在那样的地方也只能被认为是异常的景象在%NAME:1%的眼前出现
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 乍一看好像是在某个洞穴里、但地板、墙壁、天花板上随处可见的触手说明、这不是普通的洞穴
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  乍一看好像是在某个洞穴里、但地板、墙壁、天花板上随处可见的触手说明、这不是普通的洞穴
ENDIF

PRINTFORML 
IF FLAG:AUTOMODE
    PRINTFORML 明明到刚才为止还过着和平时一样的日常生活、到底自己为什么会出现在这样的地方呢…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  明明到刚才为止还过着和平时一样的日常生活、到底自己为什么会出现在这样的地方呢…
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 这么说来、听说最近遭遇神隐的人数在增加
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  这么说来、听说最近遭遇神隐的人数在增加
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 本来以为在这种地方根本就没有神隐、看来自己也遭遇了那个神隐啊
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  本来以为在这种地方根本就没有神隐、看来自己也遭遇了那个神隐啊
ENDIF

PRINTFORML 
IF FLAG:AUTOMODE
    PRINTFORML 总之先赶快从这么恶心的地方溜出去吧……即使这么想着、%NAME:1%还是觉得自己的身体不太舒服
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  总之先赶快从这么恶心的地方溜出去吧……即使这么想着、%NAME:1%还是觉得自己的身体不太舒服
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 总觉得不能按照自己的想法发挥自己的力量
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  总觉得不能按照自己的想法发挥自己的力量
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 怀着对瘴气缠绕的感觉的厌恶感、%NAME:1%开始在周围寻找出口……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  怀着对瘴气缠绕的感觉的厌恶感、%NAME:1%开始在周围寻找出口……
ENDIF



