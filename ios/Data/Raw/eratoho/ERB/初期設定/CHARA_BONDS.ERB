;仲のいいキャラ
@CHARA_BONDS
#DIM LCOUNT

	;キャラ初期設定
	FOR LCOUNT, 1, CHARANUM
		IF CFLAG:LCOUNT:5 >= 120 && CFLAG:LCOUNT:5 > CFLAG:(FLAG:95):5
			FLAG:95 = LCOUNT
		ENDIF
	NEXT
IF FLAG:95
	PRINTFORML 从远处传来了%CALLNAME:(FLAG:95)%的声音
	PRINTFORML 看来%CALLNAME:(FLAG:95)%也被卷入异变了……
ENDIF

