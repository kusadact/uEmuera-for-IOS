;固定イベント
;RETURN	0:失敗した　1:成功した　2:以後のイベントスキップ
@CHECK_FIXED_EVENT_142
;LOCAL
;0	LOOP
;1	目標値
;2	達成値
;3	戻り値
;4	TARGET
;10	Aフラグ
;11	Bフラグ
;12	Cフラグ
;13	Vフラグ

LOCAL:3 = 0

;目標値（イベント難易度）決定
LOCAL:1 = 0

LOCAL:10 = 0
LOCAL:11 = 0
LOCAL:12 = 0
LOCAL:13 = 0


;達成値決定
CALL GET_SEARCH_RATE
LOCAL:2 = RESULT


IF LOCAL:2 >= LOCAL:1
	;経験値が入るのは最初だけ
	IF FLAG:142 < 100
		CALL ADD_ABL_EXP_PT(0, 300)
		FLAG:142 += 100
		
		IF FLAG:AUTOMODE
		
			PRINTFORML 眼前経過了着不可思議形状的触手生物…
			PRINTFORML 打算退治的時候、突然被触手生物搭話了！
			PRINTFORML 
			TWAIT FLAG:AUTOMODE * 1000 , 0
			PRINTFORML 「不要動手啊、我不是会做坏事的触手。我只是来這裡尋找着要找的東西而已」
			PRINTFORML 「你%TACHI()%知道哪裡有身体非常敏感的人麼？」
			PRINTFORML 「如果能帯給我的話、会回禮非常好的東西的！」
			TWAIT FLAG:AUTOMODE * 1000 , 0
		ELSE
			PRINTFORMW  打算退治的時候、突然被触手生物搭話了！
			PRINTFORML 
			PRINTFORMW  「不要動手啊、我不是会做坏事的触手。我只是来這裡尋找着要找的東西而已」
			PRINTFORMW  「你%TACHI()%知道哪裡有身体非常敏感的人麼？」
			PRINTFORMW  「如果能帯給我的話、会回禮非常好的東西的！」
		ENDIF
	ELSE
		PRINTFORML 眼前経過了着不可思議形状的触手生物…
		PRINTFORML 「呀、又見面了。找到敏感的人了麼？」
	ENDIF
	IF ((FLAG:142 / 100) & 2)
		;A終了済み
		LOCAL:10 = 1
	ENDIF
	IF ((FLAG:142 / 100) & 4)
		;B終了済み
		LOCAL:11 = 1
	ENDIF
	IF ((FLAG:142 / 100) & 8)
		;C終了済み
		LOCAL:12 = 1
	ENDIF
	IF ((FLAG:142 / 100) & 16)
		;V終了済み
		LOCAL:13 = 1
	ENDIF
	PRINTFORML 要怎麼回答？
	IF LOCAL:10 == 0 && GET_PT_MAX_ABL(6) >= 3
		PRINTFORML [1] 帯来了A感覚很高的人
	ENDIF
	IF LOCAL:11 == 0 && GET_PT_MAX_ABL(7) >= 3
		PRINTFORML [2] 帯来了B感覚很高的人
	ENDIF
	IF LOCAL:12 == 0 && GET_PT_MAX_ABL(8) >= 3
		PRINTFORML [3] 帯来了C感覚很高的人
	ENDIF
	IF LOCAL:13 == 0 && GET_PT_MAX_ABL(9) >= 3
		PRINTFORML [4] 帯来了V感覚很高的人
	ENDIF
	PRINTFORML [0] 沒有線索
	
	IF FLAG:AUTOMODE
		TINPUT FLAG:AUTOMODE * 1000 , 0, 0, ""
	ELSE
		INPUT
	ENDIF
	IF RESULT == 1 && LOCAL:10 == 0 && GET_PT_MAX_ABL(6) >= 3
		$INPUT_LOOP_1
		PRINTFORML 要報上誰的名字？
			IF ABL:1:6 >= 3
				PRINTFORML [1] %CALLNAME:1%
			ENDIF
			IF ABL:(FLAG:10):6 >= 3
				PRINTFORML [2] %CALLNAME:(FLAG:10)%
			ENDIF
			IF ABL:(FLAG:11):6 >= 3
				PRINTFORML [3] %CALLNAME:(FLAG:11)%
			ENDIF
		INPUT
			IF RESULT == 1 && ABL:1:6 >= 3
				LOCAL:4 = 1
			ELSEIF RESULT == 2 && ABL:(FLAG:10):6 >= 3
				LOCAL:4 = FLAG:10
			ELSEIF RESULT == 3 && ABL:(FLAG:11):6 >= 3
				LOCAL:4 = FLAG:11
			ELSE
				GOTO INPUT_LOOP_1
			ENDIF
			IF FLAG:AUTOMODE
				PRINTFORML 「謝謝！　讓我触摸一下！」
			    PRINTFORML 這麼説着的触手生物突然用触手纏住了%CALLNAME:(LOCAL:4)%！
				TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
				PRINTFORMW  「謝謝！　讓我触摸一下！」
			    PRINTFORMW  這麼説着的触手生物突然用触手纏住了%CALLNAME:(LOCAL:4)%！
			ENDIF

		CALL PRINT_MESSAGE(LOCAL:4, 200, 142, CALLNAME:(LOCAL:4), "", 2, 0)
		CALL EXTASY(LOCAL:4, 0, 900, 400)
		CALL EXTASY(LOCAL:4, 0, 900, 400)
		CALL EXTASY(LOCAL:4, 0, 900, 400)
		CALL EXTASY(LOCAL:4, 0, 900, 400)
		CALL EXTASY(LOCAL:4, 0, 900, 400)
		PRINTFORML 　
		
		IF FLAG:AUTOMODE
		    PRINTFORML ……
		    PRINTFORML …………
		    PRINTFORML 触手生物的玩弄結束之後、%CALLNAME:(LOCAL:4)%露出了恍惚的表情、\@ BASE:(LOCAL:4):3 <= 0 ? 激烈的 # 隔着衣服 \@玩弄着自己的菊穴…

		    TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
		    PRINTFORMW  ……
		    PRINTFORMW  …………
		    PRINTFORMW  触手生物的玩弄結束之後、%CALLNAME:(LOCAL:4)%露出了恍惚的表情、\@ BASE:(LOCAL:4):3 <= 0 ? 激烈的 # 隔着衣服 \@玩弄着自己的菊穴…

		ENDIF

		IF TALENT:(LOCAL:4):74 == 0 || TALENT:(LOCAL:4):75 == 0 
			IF FLAG:AUTOMODE
			    PRINTFORML %CALLNAME:(LOCAL:4)%屈服于触手的玩弄、沈溺于菊穴的快感之中
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  %CALLNAME:(LOCAL:4)%屈服于触手的玩弄、沈溺于菊穴的快感之中
			ENDIF

		ENDIF
		IF TALENT:(LOCAL:4):74 == 0
			IF FLAG:AUTOMODE
			    PRINTFORML %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:74%】
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:74%】
			ENDIF

			TALENT:(LOCAL:4):74 = 1
		ENDIF
		IF TALENT:(LOCAL:4):75 == 0
			IF FLAG:AUTOMODE
			    PRINTFORML %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:75%】
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:75%】
			ENDIF

			TALENT:(LOCAL:4):75 = 1
			
			CALL ADD_HISTORY(DAY, LOCAL:4, 0, 10)
		ENDIF
			IF FLAG:AUTOMODE
			    PRINTFORML 　
			    PRINTFORML 「……呼、稍微做過頭了麼？　這是回禮、請收下吧！」
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  　
			    PRINTFORMW  「……呼、稍微做過頭了麼？　這是回禮、請收下吧！」
			ENDIF

		;◆◆
		CALL PRINT_MESSAGE(LOCAL:4, 200, 142, CALLNAME:(LOCAL:4), "", 3, 0)
		CALL ADD_ITEM(7, 1, 1)
		FLAG:142 += 200
	ELSEIF RESULT == 2 && LOCAL:11 == 0 && GET_PT_MAX_ABL(7) >= 3
		$INPUT_LOOP_2
		PRINTFORML 要報上誰的名字？
			IF ABL:1:7 >= 3
				PRINTFORML [1] %CALLNAME:1%
			ENDIF
			IF ABL:(FLAG:10):7 >= 3
				PRINTFORML [2] %CALLNAME:(FLAG:10)%
			ENDIF
			IF ABL:(FLAG:11):7 >= 3
				PRINTFORML [3] %CALLNAME:(FLAG:11)%
			ENDIF
		INPUT
			IF RESULT == 1 && ABL:1:7 >= 3
				LOCAL:4 = 1
			ELSEIF RESULT == 2 && ABL:(FLAG:10):7 >= 3
				LOCAL:4 = FLAG:10
			ELSEIF RESULT == 3 && ABL:(FLAG:11):7 >= 3
				LOCAL:4 = FLAG:11
			ELSE
				GOTO INPUT_LOOP_2
			ENDIF
			IF FLAG:AUTOMODE
				PRINTFORML 「謝謝！　讓我触摸一下！」
				PRINTFORML 這麼説着的触手生物突然用触手纏住了%CALLNAME:(LOCAL:4)%！
				TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
				PRINTFORMW  「謝謝！　讓我触摸一下！」
				PRINTFORMW  這麼説着的触手生物突然用触手纏住了%CALLNAME:(LOCAL:4)%！
			ENDIF

		CALL PRINT_MESSAGE(LOCAL:4, 200, 142, CALLNAME:(LOCAL:4), "", 2, 1)
		CALL EXTASY(LOCAL:4, 1, 900, 400)
		CALL EXTASY(LOCAL:4, 1, 900, 400)
		CALL EXTASY(LOCAL:4, 1, 900, 400)
		CALL EXTASY(LOCAL:4, 1, 900, 400)
		CALL EXTASY(LOCAL:4, 1, 900, 400)
		PRINTFORML 　
		
		IF FLAG:AUTOMODE
			PRINTFORML ……
			PRINTFORML …………
			PRINTFORML 触手生物的玩弄結束之時、%CALLNAME:(LOCAL:4)%露出了恍惚的表情、\@ BASE:(LOCAL:4):3 <= 0 ? 激烈的 # 隔着衣服 \@ 揉搓着自己的胸部…

			TWAIT FLAG:AUTOMODE * 400, 0
		ELSE
			PRINTFORMW  ……
			PRINTFORMW  …………
			PRINTFORMW  触手生物的玩弄結束之時、%CALLNAME:(LOCAL:4)%露出了恍惚的表情、\@ BASE:(LOCAL:4):3 <= 0 ? 激烈的 # 隔着衣服 \@ 揉搓着自己的胸部…
		ENDIF


		IF TALENT:(LOCAL:4):74 == 0 || TALENT:(LOCAL:4):76 == 0
			IF FLAG:AUTOMODE
			    PRINTFORML %CALLNAME:(LOCAL:4)%屈服于触手的玩弄、沈溺于胸部的快感之中
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  %CALLNAME:(LOCAL:4)%屈服于触手的玩弄、沈溺于胸部的快感之中
			ENDIF

		ENDIF
		IF TALENT:(LOCAL:4):74 == 0
			IF FLAG:AUTOMODE
			    PRINTFORML %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:74%】
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:74%】
			ENDIF

			TALENT:(LOCAL:4):74 = 1
		ENDIF
		IF TALENT:(LOCAL:4):76 == 0
			IF FLAG:AUTOMODE
			    PRINTFORML %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:76%】
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:76%】
			ENDIF

			TALENT:(LOCAL:4):76 = 1
			
			CALL ADD_HISTORY(DAY, LOCAL:4, 1, 10)
		ENDIF
			IF FLAG:AUTOMODE
			    PRINTFORML 　
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  　
			ENDIF

			IF FLAG:AUTOMODE
			    PRINTFORML 「……呼、稍微做過頭了麼？　這是回禮、請收下吧！」
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  「……呼、稍微做過頭了麼？　這是回禮、請收下吧！」
			ENDIF

		;◆◆
		CALL PRINT_MESSAGE(LOCAL:4, 200, 142, CALLNAME:(LOCAL:4), "", 3, 1)
		CALL ADD_ITEM(16, 1, 1)
		FLAG:142 += 400
	ELSEIF RESULT == 3 && LOCAL:12 == 0 && GET_PT_MAX_ABL(8) >= 3
		$INPUT_LOOP_3
		PRINTFORML 誰が名乗りでますか？
			IF ABL:1:8 >= 3
				PRINTFORML [1] %CALLNAME:1%
			ENDIF
			IF ABL:(FLAG:10):8 >= 3
				PRINTFORML [2] %CALLNAME:(FLAG:10)%
			ENDIF
			IF ABL:(FLAG:11):8 >= 3
				PRINTFORML [3] %CALLNAME:(FLAG:11)%
			ENDIF
		INPUT
			IF RESULT == 1 && ABL:1:8 >= 3
				LOCAL:4 = 1
			ELSEIF RESULT == 2 && ABL:(FLAG:10):8 >= 3
				LOCAL:4 = FLAG:10
			ELSEIF RESULT == 3 && ABL:(FLAG:11):8 >= 3
				LOCAL:4 = FLAG:11
			ELSE
				GOTO INPUT_LOOP_3
			ENDIF
			IF FLAG:AUTOMODE
			    PRINTFORML 「謝謝！　讓我触摸一下！」
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  「謝謝！　讓我触摸一下！」
			ENDIF

			IF FLAG:AUTOMODE
			    PRINTFORML 這麼説着的触手生物突然用触手纏住了%CALLNAME:(LOCAL:4)%！
			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
			    PRINTFORMW  這麼説着的触手生物突然用触手纏住了%CALLNAME:(LOCAL:4)%！
			ENDIF

			CALL PRINT_MESSAGE(LOCAL:4, 200, 142, CALLNAME:(LOCAL:4), "", 2, 2)
			CALL EXTASY(LOCAL:4, 2, 900, 400)
			CALL EXTASY(LOCAL:4, 2, 900, 400)
			CALL EXTASY(LOCAL:4, 2, 900, 400)
			CALL EXTASY(LOCAL:4, 2, 900, 400)
			CALL EXTASY(LOCAL:4, 2, 900, 400)
			PRINTFORML 
			IF FLAG:AUTOMODE
				PRINTFORML ……
				PRINTFORML …………
				PRINTFORML 触手生物的玩弄結束之時、%CALLNAME:(LOCAL:4)%露出了恍惚的表情、\@ BASE:(LOCAL:4):3 <= 0 ? 激烈的 # 隔着衣服 \@反復玩弄着自己的\@ GET_PENIS(LOCAL:4) ? 陰茎 # 陰蒂 \@…

			    TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
				PRINTFORMW  ……
				PRINTFORMW  …………
				PRINTFORMW  触手生物的玩弄結束之時、%CALLNAME:(LOCAL:4)%露出了恍惚的表情、\@ BASE:(LOCAL:4):3 <= 0 ? 激烈的 # 隔着衣服 \@反復玩弄着自己的\@ GET_PENIS(LOCAL:4) ? 陰茎 # 陰蒂 \@…

			ENDIF

			IF TALENT:(LOCAL:4):74 == 0 || TALENT:(LOCAL:4):77 == 0
				IF FLAG:AUTOMODE
					PRINTFORML %CALLNAME:(LOCAL:4)%屈服于触手的玩弄、沈溺于\@ GET_PENIS(LOCAL:4) ? 陰茎 # 陰蒂 \@的快感之中
					TWAIT FLAG:AUTOMODE * 400, 0
				ELSE
					PRINTFORMW  %CALLNAME:(LOCAL:4)%屈服于触手的玩弄、沈溺于\@ GET_PENIS(LOCAL:4) ? 陰茎 # 陰蒂 \@的快感之中
				ENDIF

			ENDIF
			IF TALENT:(LOCAL:4):74 == 0
				IF FLAG:AUTOMODE
					PRINTFORML %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:74%】
					TWAIT FLAG:AUTOMODE * 400, 0
				ELSE
					PRINTFORMW  %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:74%】
				ENDIF

				TALENT:(LOCAL:4):74 = 1
			ENDIF
			IF TALENT:(LOCAL:4):77 == 0
				IF FLAG:AUTOMODE
					PRINTFORML %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:77%】
					TWAIT FLAG:AUTOMODE * 400, 0
				ELSE
					PRINTFORMW  %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:77%】
				ENDIF

				TALENT:(LOCAL:4):77 = 1
				
				CALL ADD_HISTORY(DAY, LOCAL:4, 2, 10)
			ENDIF
				IF FLAG:AUTOMODE
					PRINTFORML 　
					PRINTFORML 「……呼、稍微做過頭了麼？　這是回禮、請收下吧！」
					TWAIT FLAG:AUTOMODE * 400, 0
				ELSE
					PRINTFORMW  　
					PRINTFORMW  「……呼、稍微做過頭了麼？　這是回禮、請收下吧！」
				ENDIF

		;◆◆
		CALL PRINT_MESSAGE(LOCAL:4, 200, 142, CALLNAME:(LOCAL:4), "", 3, 2)
		CALL ADD_ITEM(23, 1, 1)
		FLAG:142 += 800
	ELSEIF RESULT == 4 && LOCAL:13 == 0 && GET_PT_MAX_ABL(9) >= 3
		$INPUT_LOOP_4
		PRINTFORML 要報上誰的名字？
			IF ABL:1:9 >= 3
				PRINTFORML [1] %CALLNAME:1%
			ENDIF
			IF ABL:(FLAG:10):9 >= 3
				PRINTFORML [2] %CALLNAME:(FLAG:10)%
			ENDIF
			IF ABL:(FLAG:11):9 >= 3
				PRINTFORML [3] %CALLNAME:(FLAG:11)%
			ENDIF
		INPUT
			IF RESULT == 1 && ABL:1:9 >= 3
				LOCAL:4 = 1
			ELSEIF RESULT == 2 && ABL:(FLAG:10):9 >= 3
				LOCAL:4 = FLAG:10
			ELSEIF RESULT == 3 && ABL:(FLAG:11):9 >= 3
				LOCAL:4 = FLAG:11
			ELSE
				GOTO INPUT_LOOP_4
			ENDIF
			IF FLAG:AUTOMODE
				PRINTFORML 「謝謝！　讓我触摸一下！」
				PRINTFORML 這麼説着的触手生物突然用触手纏住了%CALLNAME:(LOCAL:4)%！
				TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
				PRINTFORMW  「謝謝！　讓我触摸一下！」
				PRINTFORMW  這麼説着的触手生物突然用触手纏住了%CALLNAME:(LOCAL:4)%！
			ENDIF


			CALL LOST_VIRGIN(LOCAL:4, 1, 4)
			CALL PRINT_MESSAGE(LOCAL:4, 200, 142, CALLNAME:(LOCAL:4), "", 2, 3)
			CALL EXTASY(LOCAL:4, 3, 900, 400)
			CALL EXTASY(LOCAL:4, 3, 900, 400)
			CALL EXTASY(LOCAL:4, 3, 900, 400)
			CALL EXTASY(LOCAL:4, 3, 900, 400)
			CALL EXTASY(LOCAL:4, 3, 900, 400)
			PRINTFORML 　
			
			IF FLAG:AUTOMODE
				PRINTFORML ……
				PRINTFORML …………
				PRINTFORML 触手生物的玩弄結束之時、%CALLNAME:(LOCAL:4)%露出了恍惚的表情、\@ BASE:(LOCAL:4):3 <= 0 ? 激烈的 # 隔着衣服 \@攪動着自己的膣内…
				TWAIT FLAG:AUTOMODE * 400, 0
			ELSE
				PRINTFORMW  ……
				PRINTFORMW  …………
				PRINTFORMW  触手生物的玩弄結束之時、%CALLNAME:(LOCAL:4)%露出了恍惚的表情、\@ BASE:(LOCAL:4):3 <= 0 ? 激烈的 # 隔着衣服 \@攪動着自己的膣内…
			ENDIF


			IF TALENT:(LOCAL:4):74 == 0 || TALENT:(LOCAL:4):78 == 0
				IF FLAG:AUTOMODE
					PRINTFORML %CALLNAME:(LOCAL:4)%屈服于触手的玩弄、沈溺于膣内的快感之中
					TWAIT FLAG:AUTOMODE * 400, 0
				ELSE
					PRINTFORMW  %CALLNAME:(LOCAL:4)%屈服于触手的玩弄、沈溺于膣内的快感之中
				ENDIF

			ENDIF
			IF TALENT:(LOCAL:4):74 == 0
				IF FLAG:AUTOMODE
					PRINTFORML %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:74%】
					TWAIT FLAG:AUTOMODE * 400, 0
				ELSE
					PRINTFORMW  %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:74%】
				ENDIF

				TALENT:(LOCAL:4):74 = 1
			ENDIF
			IF TALENT:(LOCAL:4):78 == 0
				IF FLAG:AUTOMODE
					PRINTFORML %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:78%】
					TWAIT FLAG:AUTOMODE * 400, 0
				ELSE
					PRINTFORMW  %CALLNAME:(LOCAL:4)%獲得了【%TALENTNAME:78%】
				ENDIF

				TALENT:(LOCAL:4):78 = 1
				
				CALL ADD_HISTORY(DAY, LOCAL:4, 3, 10)
			ENDIF
				IF FLAG:AUTOMODE
					PRINTFORML 　
					PRINTFORML 「……呼、稍微做過頭了麼？　這是回禮、請收下吧！」
					TWAIT FLAG:AUTOMODE * 400, 0
				ELSE
					PRINTFORMW  　
					PRINTFORMW  「……呼、稍微做過頭了麼？　這是回禮、請收下吧！」
				ENDIF

		CALL PRINT_MESSAGE(LOCAL:4, 200, 142, CALLNAME:(LOCAL:4), "", 3, 3)
		CALL ADD_ITEM(61, 1, 1)
		CALL ADD_ITEM(62, 1, 1)
		CALL ADD_ITEM(63, 1, 1)
		FLAG:142 += 1600
	ELSE
		PRINTFORML 「にょろ～ん…」
		PRINTFORML 無視了感到遺憾的触手生物、%CALLNAME:1%%TACHI()%重新開始了探索
		CALL PRINT_MESSAGE(1, 200, 142, CALLNAME:1, "", 4)
		CALL PRINT_MESSAGE(FLAG:10, 200, 142, CALLNAME:1, "", 4)
		CALL PRINT_MESSAGE(FLAG:11, 200, 142, CALLNAME:1, "", 4)
	ENDIF
	
	IF FLAG:AUTOMODE
		TWAIT FLAG:AUTOMODE * 500, 0
	ELSE
		WAIT
	ENDIF
	
	;同じ場所に残り続ける
	
	IF (FLAG:142 / 100) == 31
		;4種コンプリート
		IF FLAG:AUTOMODE
			PRINTFORML 「真是太厲害了！　沒想到這麼快就找到了！」
			PRINTFORML 「這是給找到了全部人的你%TACHI()%的回禮、請收下吧！」
			PRINTFORML 得到了各種各樣的道具！
			PRINTFORML 「你%TACHI()%真的是非常努力啊、現在這個【触手英雄変身器】也免費送給你喲、請收下吧！」
			PRINTFORML 得到了☆触手使役術！
			PRINTFORML 「那麼我也該回去了。你%TACHI()%用那樣的身体在這裡会很辛苦的、早点回去的好啊～」
			PRINTFORML 這麼説着的触手生物的身影消失了…
			TWAIT FLAG:AUTOMODE * 1500, 0
		ELSE
			PRINTFORMW  「真是太厲害了！　沒想到這麼快就找到了！」
			PRINTFORMW  「這是給找到了全部人的你%TACHI()%的回禮、請收下吧！」
			PRINTFORMW  「這是給找到了全部人的你%TACHI()%的回禮、請收下吧！」！
			PRINTFORMW  「你%TACHI()%真的是非常努力啊、現在這個【触手英雄変身器】也免費送給你喲、請收下吧！」
			PRINTFORMW  得到了☆触手使役術！
			PRINTFORMW  「那麼我也該回去了。你%TACHI()%用那樣的身体在這裡会很辛苦的、早点回去的好啊～」
			PRINTFORMW  這麼説着的触手生物的身影消失了…
		ENDIF

		;ユニーク武器追加パッチ・触手使役術をにょろろから入手
		CALL PRINT_MESSAGE(1, 200, 142, CALLNAME:1, "", 5)
		CALL PRINT_MESSAGE(FLAG:10, 200, 142, CALLNAME:1, "", 5)
		CALL PRINT_MESSAGE(FLAG:11, 200, 142, CALLNAME:1, "", 5)
		;全アイテム（唯一品除く）を１つずつ入手
		FOR LOCAL:0, 1, 8
			CALL ADD_ITEM(LOCAL:0, 1, 0)
		NEXT
		FOR LOCAL:0, 10, 18
			CALL ADD_ITEM(LOCAL:0, 1, 0)
		NEXT
		FOR LOCAL:0, 20, 40
			IF LOCAL:0 == 26
				CONTINUE
			ENDIF
			CALL ADD_ITEM(LOCAL:0, 1, 0)
		NEXT
		FOR LOCAL:0, 50, 65
			IF LOCAL:0 == 60 || LOCAL:0 == 64
				CONTINUE
			ENDIF
			CALL ADD_ITEM(LOCAL:0, 1, 0)
		NEXT
		
		;コンプリートでイベント消滅
		FLAG:142 = 0
	ENDIF
	
	LOCAL:3 = 1
ELSE
	;見つからなかった場合（通常は何もしない）
ENDIF

RETURN LOCAL:3

